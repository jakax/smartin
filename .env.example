# Required: Resend API key (https://resend.com/api-keys)
RESEND_API_KEY=

# Optional: where contact-form messages are delivered
CONTACT_TO_EMAIL=
# Optional: sender; needs a verified domain in Resend to send to arbitrary recipients
CONTACT_FROM_EMAIL=
